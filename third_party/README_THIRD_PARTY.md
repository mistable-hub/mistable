# Third-party components

- `third_party/pico-sdk/` (submodule, pinned revision required)
- `third_party/FreeRTOS-Kernel/` (submodule, pinned revision required)
- `third_party/waveshare_demo/` (baseline reference sources)

Use:
```bash
git submodule update --init --recursive
```
