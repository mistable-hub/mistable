# Third Party Sources

This project pins third-party code via git submodules:
- `third_party/pico-sdk`
- `third_party/FreeRTOS-Kernel`
- `third_party/waveshare_demo`

Initialize/update with:
```bash
git submodule update --init --recursive
```
