# Third-party dependencies

This project expects deterministic pinned submodules:
- `third_party/pico-sdk`
- `third_party/FreeRTOS-Kernel`
- `third_party/waveshare_demo`

Initialize with:
`git submodule update --init --recursive`
