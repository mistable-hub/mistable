# PicoDVI_or_TMDS baseline

This repository uses deterministic path S4(b): `third_party/PicoDVI_or_TMDS/`.
The in-repo implementation in `src/video/` is derived from this local baseline
for a PIO + DMA style TMDS scanout structure suitable for Phase 1 wiring.
